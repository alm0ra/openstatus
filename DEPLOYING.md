# راهنمای استقرار نسخهٔ نقطه

این راهنما برای فورک نقطه و استقرار مستقل آن است. تمام دامنه‌ها و مقادیر نمونه‌اند؛ تنظیمات واقعی زیرساخت و اعتبارنامه‌ها باید خارج از Git بمانند. اجرای استقرار تابع مجوزها و سیاست محیط مقصد است.

## معماری و محدوده

| بخش | برنامه | نیازمندی |
| --- | --- | --- |
| صفحهٔ عمومی | `apps/status-page` | libSQL، دامنه و HTTPS |
| پنل مدیریت | `apps/dashboard` | همان دیتابیس، دامنه و Secret مستقل برای ورود |
| اطلاع‌رسانی تلگرام و بله، اختیاری | `apps/status-bot` | توکن بات و دیتابیس مستقل |
| مدیریت با ایجنت، اختیاری | MCP در `apps/server` | استقرار API و اعتبارنامهٔ محدود به workspace |

PostgreSQL لازم نیست. برای صفحهٔ دستی، رخدادها و اجزای سرویس در libSQL نگهداری می‌شوند. پایش خودکار و نمودارهای اندازه‌گیری‌شده به سرویس‌های monitoring و Tinybird هم نیاز دارند؛ بالا آوردن این دو برنامه به‌تنهایی پایش خودکار ایجاد نمی‌کند.

## ۱. فایل‌های خصوصی

تنظیمات واقعی را در `.deploy-private/` نگه دارید؛ این مسیر در Git نادیده گرفته می‌شود. پوشه با دسترسی `0700` و فایل‌های حاوی رمز با دسترسی `0600` نگهداری شوند. برای runtime از Kubernetes Secret استفاده کنید؛ base64 محرمانه‌سازی نیست و فایل Secret هم نباید commit شود.

موارد زیر نباید وارد Git، context ساخت عمومی یا log شوند:

- توکن دیتابیس، رمز و هش ادمین، `AUTH_SECRET` و کلیدهای پیام‌رسان؛
- kubeconfig، اعتبارنامهٔ registry و CDN و manifestهای واقعی محیط؛
- خروجی دیتابیس و تیکت‌ها، `.local/`، `.local-db/` و `.env`؛
- فونت‌های دارای مجوز خصوصی؛ آن‌ها فقط از مسیر خصوصی به ایمیج اضافه شوند.

قبل از push، `git diff --cached` و `git status --short` را بررسی کنید. هرگز برای این فایل‌ها از `git add -f` استفاده نکنید. فایل‌های مثال upstream مقادیر نمایشی دارند و تنظیم production محسوب نمی‌شوند.

## ۲. دیتابیس

در سرویس libSQL موجود، یک دیتابیس یا namespace مستقل با توکن مختص همان دیتابیس بسازید. از دیتابیس برنامه‌های دیگر یا دیتابیس دمو استفاده نکنید. در libSQL چند namespaceای، نام میزبان بخشی از مسیریابی است؛ جایگزینی hostname با IP ممکن است namespace نادرست را انتخاب کند.

`DATABASE_URL` و `DATABASE_AUTH_TOKEN` را از محیط خصوصی در اختیار migrator بگذارید. پس از گرفتن backup و بررسی مقصد، از ریشهٔ ریپو اجرا کنید:

```sh
pnpm --filter @openstatus/db migrate
```

Migrationهای نسخه‌بندی‌شده در `packages/db/drizzle` هستند. برای production از `seed` توسعه یا `drizzle-kit push` استفاده نکنید.

راه‌اندازی اولیه باید یک کاربر مالک، workspace، عضویت مالک و صفحهٔ وضعیت بسازد. حالت تک‌ادمین حساب نمی‌سازد؛ `AUTH_ADMIN_USER_ID` باید شناسهٔ کاربر موجود و عضو workspace موردنظر باشد. اسکریپت bootstrap محیط را خصوصی نگه دارید و قبل از تکرار، idempotent بودن آن را بررسی کنید.

تنظیم صفحهٔ فارسی:

- زبان پیش‌فرض و تنها زبان: `fa`؛ تم: `default-rounded`؛
- لوگو: `/brand/noqte.svg`؛ دامنهٔ عمومی مطابق ingress؛
- حالت دستی برای اجزایی که بر اساس رخداد مدیریت می‌شوند؛
- اجزای موردنظر: وب‌سایت نقطه، تولید محتوا، API و حساب‌های حرفه‌ای؛
- بدون import تیکت واقعی یا رویداد آزمایشی به محیط عمومی.

## ۳. تنظیمات runtime

| متغیر | محل | توضیح |
| --- | --- | --- |
| `DATABASE_URL` | هر دو برنامه | آدرس دیتابیس مستقل |
| `DATABASE_AUTH_TOKEN` | هر دو برنامه | توکن محدود به همان دیتابیس |
| `SELF_HOST` | build و runtime | `true`؛ خروجی standalone هنگام build فعال می‌شود |
| `NOOP_UPTIME` | صفحهٔ عمومی | `false`؛ دادهٔ نمایشی را فعال نکنید |
| `AUTH_SECRET` | هر برنامهٔ استفاده‌کننده از Auth | مقدار تصادفی مستقل؛ بین پنل و صفحه مشترک نباشد |
| `AUTH_URL` | پنل | مثلاً `https://admin-status.example.com` |
| `AUTH_TRUST_HOST` | پنل پشت پراکسی معتبر | `true`؛ دسترسی مستقیم به origin را محدود کنید |
| `AUTH_ADMIN_ENABLED` | پنل | `true` |
| `AUTH_ADMIN_USER_ID` | پنل | شناسهٔ مالک ساخته‌شده در مرحلهٔ قبل |
| `AUTH_ADMIN_PASSWORD_HASH` | پنل | هش رمز، از Secret |
| `CRON_SECRET` | برنامه‌های وابسته به API | تصادفی و غیرخالی؛ بدون ایمیل هم لازم است |
| `TZ` | هر دو برنامه | `Asia/Tehran` |
| `HOSTNAME` / `PORT` | هر دو برنامه | `0.0.0.0` / `3000` |
| `RESEND_API_KEY` | در صورت فعال‌سازی ایمیل | کلید واقعی؛ در حالت self-host بدون ایمیل حذف شود |
| `AUTH_EMAIL_FROM` | ورود ایمیلی در صورت استفاده | فرستندهٔ تأییدشده؛ برای ورود تک‌ادمین لازم نیست |

متغیرهای `NEXT_PUBLIC_*` هنگام build در فرانت تثبیت می‌شوند. تغییر آن‌ها، از جمله لینک بات‌ها، نیاز به rebuild دارد. هیچ رمز یا توکنی را در این متغیرها قرار ندهید. نیازمندی‌های تکمیلی هر برنامه را با فایل env و اعتبارسنجی همان نسخه تطبیق دهید.

### ورود ادمین

نام کاربری `admin` است. رمز قوی تصادفی بسازید و فقط هش آن را به runtime بدهید. الگوریتم فعلی:

```text
scrypt:<32-byte salt in hex>:<64-byte derived key in hex>
N=32768, r=8, p=1, maxmem=64 MiB
```

رمز اصلی را در password manager یا فایل خصوصی ذخیره کنید. در این حالت ثبت‌نام و بازیابی رمز عمومی وجود ندارد و نشست‌ها JWT رمز‌شده با عمر هشت ساعت‌اند. هنگام تعویض رمز، `AUTH_SECRET` را نیز عوض کنید تا نشست‌های قبلی باطل شوند.

## ۴. ساخت ایمیج

از نسخه‌های pinشدهٔ ریپو استفاده کنید: Node `24.12.0` و pnpm `12.3.4`؛ نسخهٔ Deno را با `devbox.json` و CI هماهنگ نگه دارید. build باید RAM کافی داشته باشد؛ build موفق این محیط با سقف ۸ GiB و heap شش GiB انجام شده است.

```sh
pnpm install --frozen-lockfile
pnpm verify
pnpm --filter @openstatus/react build
```

سپس با env مخصوص build و `SELF_HOST=true`، برنامهٔ موردنظر را بسازید:

```sh
pnpm --filter @openstatus/status-page build
pnpm --filter @openstatus/dashboard build
```

این دستورات داخل builder مجاز اجرا می‌شوند. ساخت `@openstatus/react` پیش از Next لازم است. env ساخت باید فقط مقادیر غیرمحرمانهٔ لازم برای اعتبارسنجی داشته باشد؛ Secrets محیط واقعی را وارد builder نکنید.

اگر از `turbo prune --docker` استفاده می‌کنید، خروجی را بدون بررسی آرشیو نکنید: در این محیط فایل‌های ignored مثل `.env` و `.next` هم وارد خروجی شدند. context را از source مجاز و manifest و lockfileهای pruned بسازید و نبود فایل‌های خصوصی را بررسی کنید.

ایمیج runtime هر برنامه باید این سه مسیر را داشته باشد:

| خروجی build | مقصد در runtime |
| --- | --- |
| `apps/<app>/.next/standalone/` | `/app/` |
| `apps/<app>/.next/static/` | `/app/apps/<app>/.next/static/` |
| `apps/<app>/public/` | `/app/apps/<app>/public/` |

تمام `.env*`ها را از standalone حذف کنید. runtime با کاربر غیر root و یکی از این فرمان‌ها اجرا شود:

```sh
node apps/status-page/server.js
node apps/dashboard/server.js
```

برای هر برنامه ایمیج جدا بسازید. tag غیرقابل‌تغییر وابسته به build استفاده کنید و Deployment را به digest پین کنید. مقدار `latest` برای release مناسب نیست. commit، digest و زمان build را در سوابق خصوصی ثبت کنید.

## ۵. Kubernetes، دامنه و CDN

برای هر برنامه Deployment، Service و Ingress جدا تعریف کنید. آدرس‌های نمونه `status.example.com` و `admin-status.example.com` هستند. تنظیمات محیط واقعی در فایل‌های خصوصی می‌مانند.

- runtime غیر root، حذف capabilityها، `allowPrivilegeEscalation: false`، `seccompProfile: RuntimeDefault` و غیرفعال‌کردن mount توکن ServiceAccount؛
- requests و limits مشخص، Secret مخصوص برنامه و credential خصوصی registry؛
- برای پنل یک replica؛ محدودکنندهٔ ورود فعلی در حافظهٔ process است و بین replicaها مشترک نیست؛
- startup/liveness مناسب و readiness پنل روی `/api/auth/csrf`؛ سلامت صفحهٔ عمومی با مسیر معتبر همان دامنه بررسی شود؛
- NetworkPolicy برای ورودی ingress و خروجی لازم به libSQL، DNS و سرویس‌های موردنیاز؛
- محدودیت درخواست روی `/api/auth/callback`، همراه با محدودکنندهٔ درون برنامه؛
- حفظ چند نسخهٔ قبلی Deployment برای بازگشت.

Rate limit اینگرس معمولاً به‌ازای هر controller است، نه مجموع کلاستر. چند controller یا چند pod نباید به‌اشتباه محدودیت سراسری فرض شوند.

HTTPS را روی دامنه‌ها بررسی کنید. CDN باید مسیرهای ورود، session و API احراز هویت و پاسخ‌های خصوصی پنل را cache نکند. هنگام افزودن DNS فقط رکوردهای مربوط به این برنامه را تغییر دهید و رکوردهای دیگر zone را حفظ کنید. دسترسی مستقیم به origin باید مطابق مدل اعتماد پراکسی محدود باشد.

## ۶. بررسی پس از انتشار

1. rollout هر دو Deployment و digest نسخه را کنترل کنید.
2. صفحهٔ عمومی با HTTPS معتبر، فارسی و راست‌چین باز شود؛ لوگو و فونت‌ها بارگیری شوند.
3. حساب ناشناس نتواند workspace و اطلاعات پنل را دریافت کند.
4. رمز اشتباه و درخواست بدون CSRF رد شوند؛ ورود صحیح فقط workspace موردنظر را نشان دهد.
5. cookie نشست `Secure` و `HttpOnly` باشد و پاسخ session از CDN cache نشود.
6. محدودیت تلاش ورود را با تعداد کنترل‌شده بررسی کنید؛ مراقب قفل‌شدن موقت ورود واقعی باشید.
7. logها را بدون چاپ اعتبارنامه‌ها برای خطا بررسی کنید.

بررسی نمایش را در مرورگر و اندازهٔ موبایل انجام دهید. برای smoke test، رخداد ساختگی عمومی ثبت نکنید و برای مشترکان پیام آزمایشی نفرستید. تست ارسال فقط با حساب آزمایشی و مجوز مشخص انجام شود.

## ۷. به‌روزرسانی و بازگشت

قبل از انتشار، `pnpm verify` و تست‌های مرتبط را اجرا کنید و تغییرات migration و وابستگی‌ها را بررسی کنید. از دیتابیس backup بگیرید و امکان بازیابی آن را در محیط جدا امتحان کنید؛ وجود backup به‌تنهایی اثبات بازیابی نیست.

ترتیب انتشار: بررسی سازگاری schema، backup، migration لازم، انتشار digest جدید و smoke test. Secretها از مسیر خصوصی تأمین شوند. گردش کار CI در صورت اضافه‌شدن باید همین جداسازی را حفظ کند؛ این راهنما به معنی وجود pipeline خودکار برای استقرار نیست.

برای rollback، digest قبلی را از سوابق release انتخاب کنید. برگشت ایمیج فقط وقتی امن است که schema با نسخهٔ قبلی سازگار باشد. دیتابیس را خودکار یا کورکورانه عقب نبرید؛ restore ممکن است داده‌های پس از backup را حذف کند و به تصمیم جداگانه نیاز دارد.

توکن libSQL را قبل از انقضا بچرخانید. تعویض کلید امضای دیتابیس باید با مصرف‌کننده‌های همان دیتابیس هماهنگ باشد و به دیتابیس‌های دیگر آسیب نزند.

## ۸. قابلیت‌های اختیاری

**ایمیل:** Resend و فرستندهٔ تأییدشده و تنظیمات اشتراک workspace لازم‌اند. تا آماده‌شدن مسیر ارسال، اشتراک ایمیل را فعال نکنید. بالا بودن پنل به معنی فعال‌بودن ایمیل نیست.

**تلگرام و بله:** طبق [راهنمای بات‌ها](apps/status-bot/README.md)، worker مستقل، دیتابیس مستقل، توکن‌ها و کلید رمزنگاری شماره‌ها را تنظیم کنید. دیتابیس بات و کلید رمزنگاری باید با هم backup شوند. بات فعلی دریافت contact و ارسال اطلاع‌رسانی را انجام می‌دهد؛ فرمان مدیریت رخداد ندارد.

**مدیریت با ایجنت:** کد MCP در `apps/server/src/routes/mcp` ابزار ساخت، به‌روزرسانی و بستن رخداد و زمان‌بندی maintenance دارد. استقرار صفحه و پنل این endpoint را فعال نمی‌کند؛ API باید جدا آماده شود و credential به workspace موردنظر محدود باشد. مدیریت از تلگرام نیازمند adapter جدا با فهرست شناسه‌های مدیر مجاز و تأیید محتوای انتشار و ارسال اعلان است. مستندات پروتکل در `apps/web/src/content/pages/docs/reference/mcp-server.mdx` قرار دارد.

## رفع اشکال

| نشانه | بررسی |
| --- | --- |
| فونت یا لوگو 404 | وجود asset در runtime و مسیریابی `/fonts/` و `/brand/` روی دامنهٔ سفارشی |
| خطای module هنگام build | ساخت `@openstatus/react` پیش از Next |
| خطای env با ایمیل غیرفعال | `SELF_HOST` و متغیرهای الزامی مستقل مثل `CRON_SECRET` |
| خطای libSQL | اعتبار و انقضای توکن، namespace و Host درخواست، DNS و NetworkPolicy |
| لینک بات پس از تغییر env ثابت است | `NEXT_PUBLIC_*` نیاز به rebuild دارد |
| خطای موقت 503 در callback ورود | rate limit اینگرس و زمان رفع محدودیت |
| نمودارها واقعی نیستند | `NOOP_UPTIME=false` و تفاوت حالت دستی با monitoring واقعی |

جزئیات عملیاتی همین محیط در پوشهٔ خصوصی `.deploy-private/` نگهداری می‌شود و جزئی از مستندات عمومی نیست.

## MCP محدود برای مدیریت وضعیت

ورودی `apps/server/src/serve-mcp.ts` فقط `/mcp` و health check داخلی `/ping` را اجرا می‌کند. راه‌اندازی آن به `NODE_ENV=production`، `SELF_HOST=true` و `MCP_STATUS_ONLY=true` نیاز دارد و بدون این تنظیم‌ها متوقف می‌شود. برای origin مجاز، `OAUTH_ISSUER` را برابر origin عمومی endpoint قرار دهید.

این حالت فقط کلیدهای محلی `os_` را می‌پذیرد؛ OAuth، کلید توسعه و super-admin در این ورودی پذیرفته نمی‌شوند. کلید را با سرویس `createApiKey` برای مالک workspace بسازید تا هش، scope، انقضا و audit ثبت شوند. توکن اصلی فقط نزد کلاینت بماند؛ سرور از رکورد هش‌شدهٔ دیتابیس استفاده می‌کند. کلید `read` فقط ابزارهای خواندن را می‌بیند و کلید `write` می‌تواند رخداد و maintenance را هم مدیریت کند.

ابزارهای این حالت محدود به فهرست صفحات، اجزا، رخدادها و maintenance و ساخت/به‌روزرسانی/رفع رخداد و ایجاد maintenance هستند. این سرویس analytics ارسال نمی‌کند و برای کلید ناشناخته به Unkey مراجعه نمی‌کند. برای حالت دستی بدون اعلان، خروجی شبکه فقط به دیتابیس و DNS کافی است؛ فعال‌کردن ارسال به تنظیمات و دسترسی شبکهٔ جداگانه نیاز دارد.

Ingress باید فقط مسیر دقیق `/mcp` را به این workload بدهد، buffering را برای پاسخ Streamable HTTP غیرفعال کند و محدودیت اندازهٔ درخواست و نرخ داشته باشد. پاسخ‌ها `Cache-Control: private, no-store` دارند. پیش از انتشار، initialize، tools/list، خواندن صفحه و رد کلید نامعتبر و Origin نامعتبر را بررسی کنید.

برای اتصال Codex از MCP نوع Streamable HTTP استفاده کنید. توکن می‌تواند از متغیر محیطی `bearer_token_env_var` یا هدر خصوصی خوانده شود؛ در تنظیمات عمومی ننویسید. گزینهٔ `default_tools_approval_mode = "writes"` برای درخواست تأیید عملیات نوشتن در کلاینت است. شرح ابزارها نیز پیش‌نمایش محتوا و انتخاب صریح ارسال اعلان را الزام می‌کند؛ این قرارداد تأیید کلاینت است، نه یک مرحلهٔ تأیید مستقل در سرور.

راهنمای تنظیم کلاینت: [مستندات رسمی MCP در Codex](https://developers.openai.com/codex/mcp/).
